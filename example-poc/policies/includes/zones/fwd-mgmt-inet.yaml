---
# Management egress: tooling updates and resolution.
rules:
  - saddr: local_mgmt
    proto: tcp
    dport: web
    action: accept
  - saddr: local_mgmt
    proto: udp
    dport: dns
    action: accept
