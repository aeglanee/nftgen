---
# Default state for all rules
default: true

# MD013/line-length - Line length
MD013:
  line_length: 80
  tables: false
  # nft rulesets and command output quoted in docs are verbatim — can't wrap
  code_blocks: false
