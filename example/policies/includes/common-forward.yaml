---
# Common forward rules — never-forwardable drops + MSS clamp. Shared by routers.
rules:
  - daddr: no_forward_ipv4
    counter: true
    action: drop
  # MSS clamp for forwarded TCP (structured; statement-only rule)
  - set-mss: pmtu
